#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>
#include "shell.h"

int parse_args(){
    // char *s1 = line;
    // char ** cmds = (char **) malloc(15 * sizeof(char *));
    // char * p = s1;
    // printf("[%c]\n", p[4]);
    // printf("[%c]\n", p[5]);
    // int i;
    // for(i = 0; strncmp(&p[i], "\n", 15) == 0; i++ ){
    //   cmds[i] = strsep( &p, " " );
    // }
    // printf("[%s]\n", cmds[0]);
    // printf("[%s]\n", cmds[1]);
    // char ** args = cmds;
    // execvp(args[0], args);

  return 0;
}
